springdoc.swagger-ui.path=/swagger-ui.html

# Datasource
spring.datasource.url=${JDBC_DATABASE_URL}
spring.datasource.username=${JDBC_DATABASE_USERNAME}
spring.datasource.password=${JDBC_DATABASE_PASSWORD}

# Discuss
spring.jpa.open-in-view=true
spring.jpa.hibernate.ddl-auto=update

# Search
spring.jpa.properties.hibernate.search.backend.hosts=localhost:9200
spring.jpa.properties.hibernate.search.backend.protocol=http
spring.jpa.properties.hibernate.search.backend.username=elastic
spring.jpa.properties.hibernate.search.backend.password=WBcxrXyqsTl7cDF2b2KH
